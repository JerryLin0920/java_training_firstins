import {MEMBER_EVENTS} from "../constants/Events";

const member = (state = {
  list: [], message: ''
}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default member;